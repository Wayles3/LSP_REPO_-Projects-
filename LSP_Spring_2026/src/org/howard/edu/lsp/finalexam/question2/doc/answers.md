Design Explanation: The Template Method pattern is implemented in the BaseReport class using the final generateReport() method to enforce a specific execution order: loadData, formatHeader, formatBody, and formatFooter. Subclasses like StudentReport and CourseReport override the protected abstract methods to provide specific content while the structural workflow remains invariant. This promotes code reuse and ensures that all reports follow the same required output format.